/**
 * Application-wide custom event name constants.
 *
 * Centralises event strings so dispatch and listen sites stay in sync
 * without relying on untyped string literals.
 */

/** Fired to open the install/setup dialog from any component. */
export const OPEN_INSTALL_EVENT = 'open-install'
